# SELENE notebook dependencies
# Derived from the imports across notebooks/ and notebooks/src/.
# Unpinned by design — install latest compatible versions into the `selene` conda env.
# Standard-library modules and the local `src` package are intentionally omitted.

# --- Core scientific / ML stack ---
numpy
pandas
scipy
scikit-learn
statsmodels

# --- Plotting / visualization ---
matplotlib
seaborn
wordcloud

# --- Deep learning, NLP & LLM tooling ---
torch
transformers
datasets
sentence-transformers
peft
spacy
nltk
faiss-cpu            # use faiss-gpu instead if running on CUDA
openai
ollama
langchain-huggingface
langchain-ollama

# --- Text / document processing & web ---
regex
Unidecode
beautifulsoup4
readability-lxml
html2text
markitdown
marker-pdf
wikipedia
simhash
requests

# --- Notebook / runtime utilities ---
ipython
tqdm
PyYAML
